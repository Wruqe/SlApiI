const openChat = document.getElementById("openChat")
 if (openChat) {
     openChat.addEventListener('click', function () {
         document.querySelector('#chat').removeAttribute('class');
        })
 }

